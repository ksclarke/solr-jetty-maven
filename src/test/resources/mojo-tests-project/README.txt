==================
mojo-tests-project
==================

This is a test Maven project that tests the CreateSolrCore and DeleteSolrCore
mojos provided with the solr-jetty-maven project.  It creates its own Maven
repository in the target directory (rather than use the user's local repository).
