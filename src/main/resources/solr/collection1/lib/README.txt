This directory is for Solr filter and analyzer jars (dependencies) that aren't
available through the Maven repository.

Don't delete this README.txt file, it also serves as an indicator that the
directory should to be copied when creating a new core from the existing core. 
